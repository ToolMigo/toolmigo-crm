(()=>{const board=document.querySelector('[data-board]');if(!board)return;const csrf=document.querySelector('[name=csrfmiddlewaretoken]')?.value;let dragged=null;function counts(){board.querySelectorAll('.kanban-column').forEach(c=>c.querySelector('[data-count]').textContent=c.querySelectorAll('.task-card').length)}board.addEventListener('dragstart',event=>{dragged=event.target.closest('.task-card');if(dragged){dragged.classList.add('dragging');event.dataTransfer.effectAllowed='move'}});board.addEventListener('dragend',()=>{dragged?.classList.remove('dragging');board.querySelectorAll('.drag-over').forEach(x=>x.classList.remove('drag-over'));dragged=null});board.querySelectorAll('[data-dropzone]').forEach(zone=>{zone.addEventListener('dragover',event=>{event.preventDefault();zone.classList.add('drag-over');const cards=[...zone.querySelectorAll('.task-card:not(.dragging)')];const next=cards.find(card=>event.clientY<card.getBoundingClientRect().top+card.offsetHeight/2);next?zone.insertBefore(dragged,next):zone.appendChild(dragged)});zone.addEventListener('dragleave',event=>{if(!zone.contains(event.relatedTarget))zone.classList.remove('drag-over')});zone.addEventListener('drop',async event=>{event.preventDefault();zone.classList.remove('drag-over');if(!dragged)return;const column=zone.closest('[data-column]').dataset.column;const position=[...zone.querySelectorAll('.task-card')].indexOf(dragged)+1;counts();const response=await fetch(dragged.dataset.moveUrl,{method:'POST',headers:{'Content-Type':'application/json','X-CSRFToken':csrf},body:JSON.stringify({column_id:column,position})});if(!response.ok){alert('Verplaatsen is niet gelukt. Het bord wordt opnieuw geladen.');location.reload()}})});counts()})();
